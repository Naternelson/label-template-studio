export type Padding = {
	paddingTop: number;
	paddingRight: number;
	paddingBottom: number;
	paddingLeft: number;
};

export type Border = {
	borderWidth: number;
	borderColor: string;
	borderStyle: string;
	borderRadius: number;
};

export type Dimensions = {
	width: number;
	height: number;
};